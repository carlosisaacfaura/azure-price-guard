[pytest]
testpaths = tests
addopts =
    -v
    --strict-markers
    --html=reports/pytest-report.html
    --self-contained-html
markers =
    live: hits the public Azure Retail Prices API (deselect with -m "not live")
